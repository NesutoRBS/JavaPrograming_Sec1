/*import javax.swing.*;

public class Lab706 {
	
	public static void main(String[] args) {
		int[] nums = {25,78,41,22,36,85,37};
		int inputNum = Integer.parseInt(
				JOptionPane.showInputDialog("Input index of array:"));
		while(inputNum < 0 || inputNum > 6) {
			inputNum = Integer.parseInt(
					JOptionPane.showInputDialog("Input index of array, again:"));
		}
		JOptionPane.showMessageDialog(null,
				"Current data, nums[" + currentData(nums,inputNum) + " is " + nums[currentData(nums,inputNum)] +
				"Previous data, nums[" + prevData(nums,inputNum) + " is " + nums[prevData(nums,inputNum)] +
				"Next data, nums[" + nextData(nums,inputNum) + " is " + nums[nextData(nums,inputNum)]
				);

	}
	
	public static boolean checkIndex(int[] nums, int index) {
		
	}
	
	public static int currentData(int[] nums, int index) {
		
	}
	
	public static int prevData(int[] nums, int index) {
		
	}
	
	public static int nextData(int[] nums, int index) {
		
	}

}
*/
